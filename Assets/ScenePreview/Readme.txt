A developer looking at the scene preview should just be concerned with the files inside the API
folder.

The BackendInfrastructure folder contains helper files that are used for the preview to work,
but which will go away in production.
